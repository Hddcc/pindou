# 拼豆画板 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 拼豆画板（暂定） |
| 文档版本 | V1.1（移动/平板优先修订） |
| 文档状态 | MVP 需求初稿 |
| 面向平台 | 手机/平板优先的响应式网页应用（PWA），部署到云服务器后通过公开链接访问；电脑浏览器作为兼容平台 |
| 目标 | 用尽可能低的成本提供一个打开链接即可使用、适合触屏操作、可自由创作、可保存和可导出的拼豆图纸工具 |

## 2. 产品概述

拼豆画板是一款手机和平板优先的在线拼豆图纸编辑工具。用户通过网页链接进入即可开始使用，不需要从应用商店下载安装。用户可以选择 MARD291 色号，把颜色逐格填入不同尺寸的拼豆背景中，自由绘制任何图案，并将作品保存到本地或云端，导出为带方格背景或不带方格背景的图片。

首版优先保证“手机打开即画、触屏操作顺手、作品不丢、导出可用”。用户无需登录即可开始绘制；云保存采用登录后的可选能力，本地保存和本地文件导出始终可用。用户可以将网页添加到手机或平板主屏幕，获得接近轻应用的启动体验。

### 2.1 首版产品形态决策

首版采用响应式 Web + PWA：用户通过服务器上的公开 HTTPS 链接访问，Android 和 iOS/iPadOS 用户可以选择添加到主屏幕。这个形态只需要维护一套前端，适合复用现有云服务器，省去应用商店审核、安装包分发和多端重复开发成本。

原生 App、微信/支付宝小程序可以作为后续包装渠道，首版功能和数据结构应保持可复用，未来具备迁移空间。

## 3. 背景与问题

现有拼豆创作通常依赖图片编辑软件或纸质方格纸，存在以下问题：

- 普通绘图软件缺少拼豆色号和逐格编辑体验；
- 画布尺寸、网格和颜色信息需要手工维护；
- 图纸保存方式分散，浏览器关闭或更换设备后容易丢失；
- 导出时既需要带方格的制作图，也需要干净的成品图。

本产品用一个轻量网页工具解决以上问题，同时保留用户自由创作的空间，不提供固定模板限制创作内容。

## 4. 产品目标与成功标准

### 4.1 产品目标

1. 任何人用手机或平板打开公开链接后，可以不登录直接新建画布并开始绘制。
2. 用户可以在触屏方格画布上使用 MARD291 色号逐格绘制，并支持常见编辑操作。
3. 用户可以可靠地保存作品：未登录使用本地保存，登录后使用云保存。
4. 用户可以按需求导出带网格或不带网格的成品。
5. 产品部署在已有云服务器上即可运行，首版尽量使用免费开源组件，不依赖应用商店上架和付费第三方服务。

### 4.2 MVP 成功标准

- 手机或平板首屏打开后 3 秒内出现可编辑画布（正常网络条件下）；
- 用户从新建画布到填色不超过 2 个必要步骤；
- 在 360px 宽手机和 768px 宽平板上，工具栏、色板和画布均可正常操作且无横向溢出；
- 关闭页面再打开后，本地临时作品可以恢复；
- 100 × 100 画布上拖动填色、撤回、填充和缩放操作可正常完成；
- 导出的 PNG 在常见图片查看器中可打开，网格模式和无网格模式结果符合选择；
- 作品数据可在登录用户的其他浏览器/设备恢复；
- 无登录用户也能完整使用编辑、导出和本地文件保存功能。

## 5. 用户与使用场景

### 5.1 用户角色

| 角色 | 说明 | 主要权限 |
| --- | --- | --- |
| 游客 | 未登录的普通用户 | 新建、绘制、撤回/反撤回、本地临时保存、本地文件保存、导出 |
| 云端用户 | 已登录的普通用户 | 游客全部能力，加上云端保存、云端作品列表、跨设备恢复 |
| 管理者 | 产品拥有者 | 发布网页、维护 MARD291 色库、处理基础运行问题；首版不提供复杂后台 |

### 5.2 典型场景

1. 用户在手机上临时画一个小图，浏览器自动保存，稍后继续编辑。
2. 用户在平板上横屏制作较大的拼豆图纸，保存为本地文件并导出带网格 PNG 作为制作参考。
3. 用户登录后在手机上保存作品，到另一台平板继续绘制。
4. 用户完成作品后在手机上导出无网格 PNG，用于分享或查看最终效果。

## 6. 范围定义

### 6.1 MVP 必须包含（P0）

- 公开网页访问，游客可直接使用；
- 新建不同宽高的方格画布；
- MARD291 色号选择、搜索和当前色显示；
- 画笔逐格填色、橡皮擦、取色器、封闭区域批量填充；
- 触摸点击、触摸拖动和双指缩放；鼠标操作作为电脑兼容能力；
- 缩放、平移和适配画布；
- 撤回、反撤回；
- 自动临时保存；
- 本地保存/打开作品文件；
- 登录后的云端保存/打开；
- 导出带网格 PNG 和不带网格 PNG；
- PWA 基础能力：网页清单、主屏幕图标、编辑器资源缓存；
- 新建页面和未保存提醒；
- 空画布、加载中、保存失败、导出失败等基础状态提示。

### 6.2 后续版本（P1/P2）

- 作品分享链接和只读预览；
- 颜色数量统计、缺珠清单和采购清单；
- 参考图片导入并自动映射到 MARD291 色库；
- 多页面作品、模板库和社区作品；
- 图层、框选移动、镜像、旋转、文字工具；
- 协同编辑、评论和复杂账号体系。

### 6.3 首版明确不做

- 不限制用户只能绘制指定图案；
- 不强制接入商城、支付或广告系统；
- 不要求安装原生 App 或从应用商店下载；
- 不做专业图像编辑软件级别的滤镜和复杂图层。

## 7. 信息架构

```text
公开链接/首页（手机或平板浏览器）
├── 添加到主屏幕提示（可选）
├── 新建画布
├── 最近作品
├── 本地打开
└── 登录/账号入口

编辑器
├── 顶部：作品名称、保存状态、撤回/反撤回、导出、新建
├── 底部：触屏工具栏
├── 主区域：可缩放方格画布
└── 底部抽屉：MARD291 色板、当前色、最近使用色

导出面板
├── 带网格背景 PNG
├── 不带网格背景 PNG
└── 本地作品文件（可再次编辑）

云端作品
└── 我的作品列表、打开、重命名、删除
```

## 8. 核心功能需求

### 8.1 首页/工作台（手机/平板优先）

#### 需求

- 用户用手机或平板打开公开链接即可看到“新建画布”“本地打开”“最近作品”等入口。
- 游客不需要先注册；登录入口应保持可见但不阻挡创作。
- 页面应适配浏览器安全区域（刘海、底部手势条）和竖屏/横屏切换。
- 首次打开可显示一次“添加到主屏幕”提示；用户关闭后不重复打扰。
- 最近作品显示本地自动保存的作品，至少包含名称、画布尺寸、最后编辑时间和预览缩略图。
- 云端用户额外显示云端作品列表。

#### 验收标准

- 点击“新建画布”可进入尺寸设置；
- 点击“本地打开”可选择作品文件并恢复画布、颜色和作品名称；
- 最近作品打开后能恢复到上次保存状态；
- 没有作品时显示空状态，不出现报错页面。

### 8.2 新建画布

#### 需求

- 支持预设尺寸：16 × 16、32 × 32、64 × 64、96 × 96、128 × 128。
- 支持自定义宽度和高度，首版建议限制为 1 至 200 格，防止低端设备卡顿。
- 可设置作品名称，默认名称为“未命名拼豆图”。
- 新建时默认显示方格背景，空格不含颜色。
- 当当前作品存在未保存修改时，新建前提示“保存并新建”“直接新建”“取消”。

#### 验收标准

- 输入非法尺寸、空值或超出范围时给出明确提示，不能创建；
- 创建成功后画布尺寸与设置一致；
- 新建操作不会覆盖原作品，除非用户明确选择直接放弃。

### 8.3 方格画布与视图（触屏核心）

#### 需求

- 画布由宽 × 高的方格组成，每格对应一颗拼豆。
- 空格显示为浅色背景；已填色格显示所选 MARD291 颜色。
- 支持放大、缩小、100% 显示、适配窗口；手机和平板支持双指缩放和按钮缩放，电脑兼容鼠标滚轮。
- 大画布支持平移，画布缩放时网格线和色块位置保持稳定，不发生错位。
- 选中格应有清晰描边；当前工具和当前颜色始终可见。
- 画布在手机竖屏、手机横屏、平板竖屏和平板横屏下均可使用。
- 绘画时应区分“单指绘画”和“多指视图操作”：单指用于点击/拖动填色，双指用于缩放/平移，避免误触浏览器页面滚动。
- 画布应使用稳定的方格尺寸；缩放后单格仍可被手指准确点击，必要时显示放大镜或十字定位辅助。

#### 验收标准

- 100 × 100 画布在手机和平板放大后可精确点击单格；
- 缩放和平移不会修改任何颜色；
- 画布尺寸较大时，界面仍能访问工具栏和保存/导出入口，且不会被系统手势区域遮挡。

### 8.4 MARD291 色板

#### 需求

- 内置 MARD291 颜色数据，至少包含：色号、颜色名称（若有）、显示色值、可视色块。
- 色板支持按色号/名称搜索；支持按色号排序。
- 点击色块后设置为当前颜色，并显示色号和名称。
- 提供最近使用颜色，减少重复查找；最近使用数量建议为 12 个。
- 当前颜色必须有文字信息，不能只靠颜色区分。
- 色库数据由管理者可替换或更新，更新不应破坏历史作品中已保存的色号。

#### 验收标准

- 用户可以通过色号搜索并选中目标颜色；
- 选中颜色后，画笔填色结果与当前色一致；
- 色盲或低对比度环境下仍可通过色号识别颜色。

### 8.5 编辑工具

#### 8.5.1 画笔

- 点击单格填色；
- 在连续格上拖动可连续填色；
- 一次连续拖动视为一个历史操作，便于撤回；
- 填入已有相同颜色时不产生无意义历史记录。

#### 8.5.2 橡皮擦

- 点击或拖动清空格子；
- 清空后的格子恢复为空格状态；
- 橡皮擦操作支持撤回。

#### 8.5.3 取色器

- 点击已填色格后，将该格颜色设置为当前颜色；
- 点击空格时不改变当前颜色，并给出轻提示或保持原状态。

#### 8.5.4 封闭区域批量填充（油漆桶/魔法棒）

- 用户选择颜色后点击目标格；
- 从目标格开始，将相连且颜色相同的区域填充为当前颜色；
- 空白区域可以被填充；已有颜色区域也可以整体替换；
- “相连”默认指上下左右四方向，不包含对角线；
- 一次填充视为一个历史操作；
- 对大面积区域操作时显示处理中状态，完成后允许继续编辑。

#### 8.5.5 视图工具

- 缩放、平移和适配窗口不改变作品数据；
- 工具切换后有选中状态；
- 手机端点击和拖动不应误触页面滚动。

### 8.6 撤回与反撤回

#### 需求

- 提供撤回和反撤回按钮；按钮在不可用时置灰。
- 建议至少保留最近 100 个编辑步骤，包含画笔、橡皮擦和区域填充。
- 新建作品、打开作品、保存和导出不应清空当前历史记录；打开另一作品时开始新的历史记录。
- 撤回/反撤回后画布和颜色统计（若后续加入）同步更新。
- 桌面端可提供 Ctrl/Cmd + Z 和 Ctrl/Cmd + Shift + Z 快捷键，移动端以按钮为主。

#### 验收标准

- 连续执行“填色 → 撤回 → 反撤回”后，目标格最终恢复原颜色；
- 油漆桶一次填充可以整体撤回；
- 没有可撤回或反撤回内容时按钮不会产生异常操作。

### 8.7 临时保存与本地保存

#### 临时保存

- 用户开始编辑后自动保存到当前浏览器的本地存储；
- 建议在编辑停止约 1 秒后保存，并至少每 10 秒做一次兜底保存；
- 页面关闭、刷新或意外中断后，重新进入可看到“恢复上次未完成作品”提示；
- 临时保存失败时给出提示，不能阻塞继续绘图。

#### 本地作品文件

- 用户可以主动点击“保存到本地”，下载可再次编辑的作品文件，建议使用 `.pindou` 扩展名；
- 文件至少包含作品名称、画布宽高、每格颜色对应的 MARD291 色号、版本号和最后修改时间；
- 用户可以通过“本地打开”重新载入该文件；
- 本地文件保存与 PNG 导出互不替代：作品文件用于继续编辑，PNG 用于查看和分享。

#### 验收标准

- 浏览器刷新后，临时作品可恢复；
- 下载的作品文件可以在同一浏览器重新打开；
- 文件打开失败时显示可理解的错误信息，不破坏当前正在编辑的作品。

### 8.8 云端保存

#### 需求

- 游客点击云保存时，提示登录或注册；登录前可继续使用本地保存。
- 登录后支持“保存到云端”“另存为”“打开云端作品”“重命名”“删除”。
- 保存状态显示为“已保存”“保存中”“保存失败”；
- 编辑停止后自动保存云端的策略建议可配置，首版可采用手动云保存加轻量自动保存；
- 保存时以作品文件中的 MARD291 色号为准，色库显示名称变化不影响历史作品；
- 网络中断时保留本地版本，恢复网络后允许重试上传；
- 同一作品在多个设备同时修改时，首版提示用户选择保留本地或云端版本，不做实时协同合并。

#### 验收标准

- 登录用户在设备 A 保存后，能在设备 B 登录并打开作品；
- 云保存失败时用户能看到失败原因或重试入口；
- 删除云端作品前需要确认，删除后列表和预览同步消失。

### 8.9 导出

#### 需求

- 导出入口提供两种明确选项：
  1. **带方格背景 PNG**：保留完整画布尺寸、空格、格线和已填色格，适合照图制作；
  2. **不带方格背景 PNG**：保留完整画布尺寸和已填色结果，不绘制格线；空白格默认为透明，适合分享成品。
- 导出前显示预览和画布尺寸；
- 导出文件名默认使用作品名称，可自动附加 `-grid` 或 `-clean`；
- 导出使用当前作品状态，若存在未保存修改，应先完成本地临时保存；
- 低端设备导出较大画布时显示处理中状态，完成后提供下载。

#### 验收标准

- 带方格 PNG 能看到规则方格线，格线与每格颜色边界对齐；
- 不带方格 PNG 不含格线，空白格为透明或符合产品设置的透明背景；
- 两种 PNG 的像素尺寸与画布尺寸按统一格子大小换算，且导出后图案位置一致；
- 取消导出不会修改作品。

### 8.10 新建、打开与离开保护

- 顶部提供“新建”“本地打开”“云端作品”入口；
- 当前作品有未保存修改时，切换作品、刷新或离开前提示保存；
- 保存完成后提示应自动消失，不反复打扰用户；
- 重新进入应用时，优先显示最近编辑作品和恢复入口。

## 9. 关键用户流程

### 9.1 游客创作并导出

```text
手机/平板打开公开链接（可添加到主屏幕） → 新建画布 → 选择尺寸 → 选择 MARD291 色号
→ 画笔/橡皮擦/油漆桶编辑 → 自动临时保存
→ 导出 → 选择带网格或不带网格 → 下载 PNG
```

### 9.2 本地保存并恢复

```text
编辑作品 → 保存到本地 → 下载 .pindou 文件
→ 之后点击本地打开 → 选择文件 → 恢复可编辑作品
```

### 9.3 云端保存并跨设备继续

```text
编辑作品 → 点击云保存 → 登录/注册 → 保存到云端
→ 其他设备登录 → 打开云端作品 → 继续编辑
```

## 10. 页面与交互要求

### 10.1 编辑器布局

- 手机端：顶部精简操作栏 + 中央画布 + 底部工具栏，色板以全宽底部抽屉展示；
- 平板端：顶部操作栏 + 中央画布 + 可折叠侧边色板，横屏时优先使用更宽的画布区域；
- 桌面端：保留顶部操作栏、左侧工具栏、中央画布和右侧色板的兼容布局；
- 画布区域占据主要空间，工具和色板不能遮挡当前编辑格；
- 保存状态、作品名称和当前颜色始终能被用户找到。

### 10.2 状态反馈

- 轻量操作使用短暂提示，例如“已保存到本地”“已复制颜色”；
- 长时间操作使用进度或加载状态；
- 错误提示说明下一步行动，例如“网络不可用，已保留本地版本，可稍后重试”；
- 破坏性操作（放弃未保存修改、删除云作品、清空画布）必须二次确认。

### 10.3 可用性与兼容性

- 首要支持最新版 Android Chrome、iOS Safari、iPadOS Safari；同时兼容最新版 Chrome、Edge、Firefox 桌面浏览器；
- 手机最小建议支持宽度 360px，平板最小建议支持宽度 600px；
- 颜色选择不能只依赖色彩，必须显示色号；
- 按钮和工具触摸目标建议不小于 44 × 44px；
- 交互目标区域适合触摸，画布格子过小时以缩放后精确编辑为主；
- 页面支持横竖屏切换，切换时保留当前画布位置、缩放比例和编辑状态；
- 浏览器返回、系统切后台、锁屏恢复等场景不能清空当前画布；
- 网络短暂中断不应导致当前画布清空。

## 11. 数据要求

### 11.1 作品数据

```text
作品名称
画布宽度、画布高度
每个非空格子的坐标与 MARD291 色号
作品文件格式版本
创建时间、最后修改时间
缩略图（可选，供作品列表展示）
```

建议使用稀疏数据保存非空格子，避免大画布全部存储空值，降低本地和云端空间占用。

### 11.2 云端账号与作品

- 账号只保存实现云端同步所需的最少信息；
- 用户只能看到和操作自己的私有云作品；
- 云端删除、账号注销和数据导出应符合后续隐私政策；
- 作品文件版本需要可向后兼容，色号作为稳定主键保存。

## 12. 部署方式与低成本约束

### 12.1 推荐形态

- 采用手机/平板优先的响应式网页应用，并提供 PWA 能力（网页清单、图标、Service Worker）；
- 用户通过一个公开 HTTPS 链接访问，例如 `https://你的域名/`，无需应用商店审核和安装包分发；
- Android Chrome 和 iOS/iPadOS Safari 均可“添加到主屏幕”，主屏幕图标直接打开编辑器；
- 桌面浏览器保留基本兼容性，方便管理者维护色库和处理问题，但不作为首要设计目标。

### 12.2 云服务器部署建议

首版可以直接部署在现有云服务器的一台实例上：

1. 使用 Nginx 或 Caddy 提供 HTTPS 和静态网页文件；
2. 前端编辑器、临时保存、PNG 渲染和本地文件读写均在用户手机/平板完成；
3. 仅为登录、云端作品增删改查提供轻量 API；
4. 云端作品首版保存为 SQLite 或同等轻量数据库中的结构化数据，不默认上传大尺寸图片；
5. 用定期备份复制数据库文件，出现数据量增长后再迁移到独立数据库或对象存储；
6. 使用 Let's Encrypt 免费证书，域名可使用已有域名或低价域名；公开 IP 仅用于测试，正式使用建议配置域名和 HTTPS。

### 12.3 免费优先原则

- 优先复用用户已有云服务器，不新增必须购买的运行服务；
- 采用开源前端框架、开源图标和免费证书；
- 访客不强制创建账号，降低认证和数据库压力；
- 云端只存作品结构数据和必要缩略图，不默认存储导出的 PNG；
- 首版不引入实时协同、社区审核、图片 AI 识别、短信登录和支付系统；
- 访问量较小时，静态资源、轻量 API 和 SQLite 可在同一台服务器运行；
- 需要关注云服务器流量、磁盘、备份和短信/邮件服务的免费额度，超出额度前再评估扩容。

### 12.4 公开访问验收

- 通过手机流量和 Wi-Fi 均能打开公开链接；
- 未登录用户可以完成新建、绘制、临时保存、本地保存和导出；
- 页面刷新或重新打开后，PWA/浏览器能恢复最近作品；
- HTTPS 证书有效，浏览器不会显示不安全警告；
- 服务器重启后，网页和已保存的云端作品仍可访问；
- 管理者可以仅通过替换色库数据更新 MARD291 颜色，不需要修改用户作品。

## 13. 权限与隐私

- 游客作品默认仅保存在本地，不上传云端；
- 云端作品默认私有，只有用户主动分享时才生成公开链接（分享属于 P1）；
- 应在首次使用云保存时说明保存的数据类型和删除方式；
- 账号密码、登录凭证和云端作品访问必须使用安全传输；
- 本地文件由用户自行管理，应用不应读取与作品无关的本地文件内容。

## 14. 异常与边界情况

| 场景 | 预期处理 |
| --- | --- |
| 输入 0、负数、非数字或过大尺寸 | 阻止创建并提示合法范围 |
| 浏览器不支持本地存储或空间不足 | 提示用户下载本地作品文件，并允许继续尝试导出 |
| 云保存网络失败 | 保留本地版本，显示失败状态和重试入口 |
| 打开损坏或非本产品文件 | 提示文件无效，当前作品保持不变 |
| 导出大画布耗时较长 | 显示处理中，避免重复点击，完成后提供下载 |
| 油漆桶点击空白大区域 | 按四方向连续空格填充，不能超出画布边界 |
| 用户刷新或关闭页面 | 先依赖最近一次临时保存，重启后提供恢复选项 |
| 色库升级或色名变化 | 历史作品继续按原色号显示和编辑 |
| 多设备同时编辑同一云作品 | 首版提示版本差异，由用户选择本地或云端版本 |

## 15. 版本规划

### V1.0 MVP

公开 HTTPS 访问、游客创作、MARD291 色板、可变尺寸方格画布、画笔/橡皮擦/取色器/油漆桶、撤回/反撤回、临时保存、本地作品文件、登录后云保存、两种 PNG 导出、手机/平板触屏适配、PWA 基础能力。

### V1.1

分享链接、只读预览、颜色数量统计、缺珠清单、更多画布预设、作品重命名和最近作品体验优化。

### V2.0

参考图片转拼豆图、模板和作品库、镜像/旋转/框选、多页面作品、社区互动或协同编辑。

## 16. 验收清单

- [ ] 未登录用户可以公开访问并直接新建画布；
- [ ] 支持预设和自定义宽高，尺寸校验有效；
- [ ] MARD291 色号可搜索、选中、显示和用于填色；
- [ ] 画笔、橡皮擦、取色器和四方向油漆桶可用；
- [ ] 画布支持缩放、平移和移动端触摸；
- [ ] 撤回/反撤回覆盖普通填色、擦除和区域填充；
- [ ] 刷新或重开后可以恢复临时保存作品；
- [ ] 可以下载并重新打开本地 `.pindou` 文件；
- [ ] 登录用户可以保存、打开和删除云端作品；
- [ ] 云端失败时不会清空本地编辑内容；
- [ ] 可以分别导出带方格和不带方格的 PNG；
- [ ] 手机和平板通过公开 HTTPS 链接可以直接访问，不依赖应用商店安装；
- [ ] Android Chrome、iOS Safari、iPadOS Safari 在竖屏和横屏下通过基础编辑验收；
- [ ] 双指缩放、单指绘画、底部工具栏和色板抽屉不会互相误触；
- [ ] 用户可以将网页添加到主屏幕，重新打开后能恢复最近作品；
- [ ] 服务器重启后公开链接仍可访问，云端作品数据未丢失；
- [ ] 新建、打开、放弃修改和删除等操作有必要的确认提示；
- [ ] 关键功能在 Chrome/Edge/Safari/Firefox 及主流手机浏览器通过手动验收；
- [ ] 在 100 × 100 画布上完成基础编辑、保存和导出测试。

## 17. 待确认事项

以下事项不影响 PRD 的 MVP 方向，但在进入设计或开发前需要确认：

1. MARD291 色库的权威来源、色号数量和授权使用范围；
2. 不带网格导出时，空白格是否统一使用透明背景；本文默认透明；
3. 云保存的登录方式（邮箱、第三方登录或其他轻量方案）；
4. 服务器现有配置、域名和可用的免费额度；
5. 是否需要在 V1.0 就提供作品分享链接；本文将其放入 V1.1。
